class Solution(object):
    def countKDifference(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
        c=0
        for i in nums:
            for j in nums:
                if (i-j)==k:
                    c+=1
        return c
