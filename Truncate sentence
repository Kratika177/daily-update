class Solution {
    public String truncateSentence(String s, int k) {
        String[] trunc=s.split(" ");
        String s1="";
        for(int i=0;i<k;i++)
        {
            s1+=trunc[i]+" ";
        }
        s1=s1.substring(0,s1.length()-1);
        return s1;
    }
}
